name: test

channels:
  - conda-forge

dependencies:
  - python=3.6
  # required
  - pandas
  - pyproj>=2.1.3
  # testing
  - pytest
  - pytest-cov
  - codecov
  # pip
  - pip
  - pip:
      - untangle>=1.1.1

